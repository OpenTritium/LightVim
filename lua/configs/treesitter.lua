return {
    highlight = {enable = true}, -- 高亮
    indent = {enable = true}, -- 缩进
    ensure_installed = {"cpp"}, -- 确保安装
    auto_install = true, -- 自动安装缺失的组件
    sync_install = true -- 同步安装全部确保要安装的组件
}
